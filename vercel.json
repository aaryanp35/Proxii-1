{
  "installCommand": "cd proxii-frontend && npm install",
  "buildCommand": "cd proxii-frontend && npm run build",
  "outputDirectory": "proxii-frontend/dist",
  "rewrites": [
    {
      "source": "/api/score/:zipcode",
      "destination": "/api/score.js"
    }
  ]
}
