{
  "buildCommand": "npm --prefix proxii-frontend ci && npm --prefix proxii-frontend run build",
  "outputDirectory": "proxii-frontend/dist",
  "rewrites": [
    {
      "source": "/api/score/:zipcode",
      "destination": "/api/score.js"
    }
  ]
}
